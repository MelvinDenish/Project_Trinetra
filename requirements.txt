# Pinned for the submission/judge environment (Python 3.11 / 3.12, Linux, CPU-only).
# Dev machine used newer wheels on Python 3.14; these pins are what Stage-3 Docker reproduces.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
torch==2.3.1
sentence-transformers==3.0.1
transformers==4.42.4
pyyaml==6.0.2
# Demo only (not needed for the ranking step):
streamlit==1.37.1
