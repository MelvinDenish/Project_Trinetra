# EXACT versions used to produce and validate the submitted CSV (shipping what we
# actually ran, not untested guesses). Verified on Python 3.14 (Windows, dev); the
# Dockerfile reproduces on Python 3.11 (Linux) — these versions all have py3.11
# Linux wheels. Install CPU torch from the PyTorch CPU index (see Dockerfile/README):
#   pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
numpy==2.4.2
pandas==2.3.3
scipy==1.17.0
scikit-learn==1.8.0
torch==2.10.0
sentence-transformers==5.2.3
transformers==5.5.0
pyyaml==6.0.3
# Demo only (not needed for the ranking step):
streamlit==1.58.0
